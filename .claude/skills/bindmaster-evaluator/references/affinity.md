# Affinity among binders (Part N)

> **Scaffold.** `TODO:` flesh out.

- Composite `ipsae_min × |dG/dSASA|` (`binder-compare affinity`). Two-stage ranks binder-vs-
  non-binder; this ranks affinity *among* binders.
- Rosetta InterfaceAnalyzer runs in the **BindCraft** conda env — PyRosetta is present there on
  every BindCraft platform incl. aarch64/Spark (NOT x86-gated).
- `TODO:` when the Rosetta cost is worth it (only on the post-two-stage shortlist); PRODIGY note.
